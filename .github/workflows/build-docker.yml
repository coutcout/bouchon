name: Docker build

on:
  push:
    branches:
      - feature/cicd

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: List all files
        run: |
          ls
      - name: Get version
        id: get_version
        run: |
          BUILD_VERSION=$(grep "^version" build.gradle | sed -E 's/^version.*?\x27(.*?)\x27/\1/')
          echo "version $BUILD_VERSION"
          echo "project_version=$BUILD_VERSION" >> $GITHUB_ENV
          IS_SNAPSHOT==[[$BUILD_VERSION -eq *-SNAPSHOT]]
          echo "is_snapshot=$IS_SNAPSHOT" >> $GITHUB_ENV
      - name: print version
        run: |
          echo "Version ${{env.project_version}} - ${{env.is_snapshot}}"
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Gradle - Prepare build
        uses: gradle/gradle-build-action@v2
      - name: Gradle - Build
        run: gradle build
      - name: Publish Unit Test Results
        uses: EnricoMi/publish-unit-test-result-action@v1
        if: always()
        with:
          files: build/test-results/junit/**/*.xml
      - name: Codecov
        uses: codecov/codecov-action@v2
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: build # optional
      - name: List all files
        run: |
          ls
      - name: Docker - login
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Docker - build & push
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: coutcout/bouchon:0.0.2-SNAPSHOT
          build-args: |
            JAR_FILE=./build/libs/bouchon-0.0.1-SNAPSHOT.jar
            CONFIG_FOLDER=./config
        
       
